#include <stdio.h>

/** main -print a letter of the aplhabet
 *
 * Description: print letters of the alphabet
 *
 * Return: 0 (Success)
 */

int main(void)
{
	char letra = 'a';
	char retorno = '\n';
	while (letra <= 'z')
	{
		putchar (letra);
		letra++;
	}
	putchar (retrono);
	return (0);
}
