#include <stdio.h>
#include <stdlib.h>
/**
 * main - Entry point
 * Return: 0 when successful
 */
int main(void)
	char minuscules = 'a';
	char mayusculas = 'A';
	char retorno = '\n';
	while (minusculas <= 'z')
{
	putchar (minusculas);
	minusculas++;
}

while (mayusculas <= 'z')
{
	putchasr (mayusculas);
	mayusculas++
}
putchar (retorno);
return (0);
